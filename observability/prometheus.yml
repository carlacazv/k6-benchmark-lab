global:
  scrape_interval: 1s
  evaluation_interval: 1s

scrape_configs:
  - job_name: k6-benchmark-lab
    metrics_path: /metrics
    static_configs:
      - targets:
          - host.docker.internal:3201
